apiVersion: eksctl.io/v1alpha5
kind: ClusterConfig

metadata:
  name: streaming-app-cluster
  region: ap-south-1

managedNodeGroups:
  - name: streaming-app-nodes
    instanceType: t3.medium
    desiredCapacity: 2
    minSize: 2
    maxSize: 3
    volumeSize: 20
    volumeType: gp3